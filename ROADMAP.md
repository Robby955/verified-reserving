# Roadmap

## Current priorities

1. ODP distributional layer: add process simulators and prove bootstrap properties only under explicit distributional assumptions.
2. Observable CDR: state any resampling or linear approximation as a separate assumption before deriving an approximation result.
3. Bühlmann-Straub: add structural-parameter estimation after the conditional-moment model.
4. Bornhuetter-Ferguson: add a finite positive-variance witness for the prediction-error results.
5. Röhr: extend the development-horizon results only when a primary source supports the additional aggregation formulas.

## Build history and module-level gaps

Each item records what was built, the remaining local gaps and the mathlib tools involved.

1. DONE 2026-08-25 (Stochastic.lean, Variance.lean, Independence.lean). `RandomTriangle` carries the random cumulative triangle and a monotone filtration `D`; `RowsGenerateD` separately states that `D k` is the join of the row histories through `k`. The source assumptions are separate predicates:
   (M1) `μ[C i (k+1) | D k] = f k * C i k` a.e.;
   (M2) accident years independent (`RowsIndep`, implemented with `ProbabilityTheory.iIndep` over the row sigma-algebras);
   (M3) `μ[(C i (k+1) - f k * C i k)^2 | D k] = σ² k * C i k` a.e.
   Tools: `MeasureTheory.condExp`, `ProbabilityTheory.iIndep`.
2. DONE 2026-08-24 (Stochastic.lean, `condExp_fhatRv`). Theorem 2a (Mack 1993): `μ[fhat k | D k] = f k` under (M1) in the `D_k`-conditioned form, on `{S_k ≠ 0}`, with integrability hypotheses. Tools used: `condExp_mul_of_stronglyMeasurable_left`, `condExp_finsetSum`, `Finset.stronglyMeasurable_sum`. The passage from Mack's row-conditioned assumption to this `D_k` form is item 10a.
3. DONE 2026-08-24 (Stochastic.lean, `condExp_fhatRv_mul`, `integral_fhatRv_mul`, `integral_fhatRv`). Theorem 2b: `E[fhat j * fhat k | D j] = f j * f k` for `j + 1 ≤ k`, and the unconditional forms on a probability space. Tools used: `condExp_condExp_of_le`, `condExp_smul`, `integral_condExp`, `StronglyMeasurable.mono`. Required adding `D_mono : Monotone D` to `RandomTriangle`.
3b. DONE 2026-08-25 (Ultimate.lean). Mack's Theorem 1: `condExp_C_of_Mack1` (iterated (M1)), `condExp_ChatRv` (partial chain-ladder products), `condExp_ultimate_eq` (the ultimate is conditionally unbiased). Tools: `condExp_of_stronglyMeasurable`, `condExp_condExp_of_le`, `condExp_smul`, induction on the number of factors.
3c. DONE 2026-08-25 (Variance.lean). (M3) and (M2') as definitions; `condExp_sq_fhatRv_sub`: E[(fhat k - f k)^2 | D k] = σ² k / S k on {S k ≠ 0}. Tools: `condExp_mul_of_stronglyMeasurable_left`, `condExp_finsetSum` twice, `Finset.sum_ite_eq'`.
4. DONE 2026-08-25 (SigmaUnbiased.lean): `weighted_sq_dev_eps`, `condExp_wssRv` (E[W_k | D_k] = (n-k-2) σ²), `condExp_sigma2Rv` (E[σ̂²_k | D_k] = σ² for k+3 ≤ n). Original plan for reference:
   Unbiasedness of `sigma2 k` under (M1)-(M3): uses `weighted_sq_dev_at_fhat` from the deterministic layer plus conditional variance of the weighted mean. This is where the `(n-k-2)` degrees of freedom are justified.
5. DONE 2026-08-25 (ProcessVariance.lean, Msep.lean): conditional second moments and tower recursion; `procVar` with closed form; `condVar_C_eq_procVar`; `condExp_sq_sub_of_stronglyMeasurable` (exact MSEP decomposition); `condMsep_eq` (Theorem 3, exact form, conditioned on the observed-data σ-algebra with the "no extra information about row i" hypotheses named); `mackProcess`, `mackEstimation` as definitions. Still to build: the identity BBMW/Murphy = Mack + explicit term (catalogue rows) and a non-vacuity witness (both done, items 9 and 10a). Original plan for reference:
   Process variance of `C i (n-1)` given the data: iterated conditional variance along the row, giving `Ĉ² ∑ σ²_k/(f_k² Ĉ_{ik})` after plugging estimates. Tool: law of total variance for condExp (prove as a lemma if absent).
6. DONE 2026-08-25 (Msep.lean): Estimation error. Mack's step replaces `(f̂_k - f_k)²` by its conditional expectation `σ²_k / S_k` and drops cross terms using 2b. State the estimator as Mack states it (the "conditional resampling" approximation) rather than as an exact identity; the exact conditional MSEP is a different object (BBMW 2006), and the catalogue records both.
7. DONE 2026-08-25 (Msep.lean): Theorem 3 (Mack 1993). Assemble 5 and 6 into `msep` as defined in ChainLadder.lean. Then the Corollary for the total reserve with the cross terms.
8. DONE 2026-08-25 (Recursion.lean): Mack 1999 recursion equals the 1993 closed form (α = 1, unit weights): a deterministic induction on `k` using `Chat_succ`; no probability needed. This is the first catalogue row and can be done immediately.
9. DONE 2026-08-25 (Catalogue.lean): Mack vs BBMW/Murphy conditional-resampling term: Mack ≤ BBMW, exact difference, agreement for a single factor, second-order bound. Non-vacuity witness in Test/Witness.lean (CI). Remaining catalogue work: formalize R ChainLadder option semantics. Röhr's linearized form is item 10c, the random witness is item 10a, and the tail-factor conventions are in item 11. Original plan for reference:
   Catalogue rows: Murphy 1994 and BBMW 2006 (extra cross-product terms): prove `msep_BBMW = msep_Mack + (explicit nonnegative term)` and exhibit a triangle with the term nonzero; Röhr 2016 linearized form; the R ChainLadder "Independence" option; the σ_{n-2} extrapolation rule stated as a definition and never used by any theorem.

10a. DONE 2026-08-25 (Independence.lean). The passage from Mack's row-conditioned assumptions to the `D_k` form. `condExp_sup_of_indep`: conditioning on an independent enlargement changes nothing, `E[f | m₁ ⊔ m₂] = E[f | m₁]` when `m₂` is independent of a σ-algebra carrying both `f` and `m₁`; mathlib has only the case `m₁ = ⊥` (`condExp_indep_eq`), so the join version is proved from `ae_eq_condExp_of_forall_setIntegral_eq` and `MeasurableSpace.induction_on_inter` over the π-system of rectangles. Applied to the row σ-algebras: `mack1_of_mack1Row`, `mack3_of_mack3Row`, and `mack2'_of_rows`, which derives the cross-term assumption (M2') from (M2) rather than assuming it. Tools used: `ProbabilityTheory.iIndep`, `indep_iSup_of_disjoint`, `condExp_indep_eq`, `condExp_condExp_of_le`. DONE 2026-08-25 (Test/IndependenceWitness.lean): the eight-outcome model has `RowsIndep`, uses the row-generated filtration exactly, satisfies `Mack1Row`, and instantiates `mack1_of_mack1Row` and `mack2'_of_rows` on genuinely random claims.
10b. DONE 2026-08-25 (CDR.lean): the one-year claims development result of Merz and Wüthrich (2008). `RandomTriangle.trueCDR` (the true CDR as a difference of conditional expectations of the ultimate); `condExp_trueCDR_eq_zero` (conditional mean zero, tower property only); `condExp_C_of_Mack1_at`, `condExp_C_ultimate_of_Mack1` ((M1) iterated from an arbitrary development year); `trueCDR_eq` (the true CDR is `∏_{j>k} f_j` times the one-step residual `f_k C_{i,k} - C_{i,k+1}`); `obsCDR`, `RandomTriangle.obsCDRRv` and `obsCDR_eq_reserve_sub` (the observable CDR as a definition plus its reserve form). Still open: the mean squared error of prediction of the observable CDR, which needs the Merz-Wüthrich approximation step (estimated factors treated as resampled, second-order residual terms dropped) stated as its own definition, the calendar-year filtration alongside the development-year one, and a non-vacuity instantiation of the CDR theorems on the witness model.

10c. DONE 2026-08-25 (Rohr.lean): catalogue row 3, Röhr (2016) error-propagation form. `rohrMsep_eq_msep` (the linearized single-accident-year formula is Mack's 1993 closed form, no side condition), `rohrProcess_eq_mackProcess` (the relative process term is `procVar` with the estimators plugged in), `rohrParameter_eq_mackEstimation`, `bbmwEstimation_sub_rohrParameter` (the linearization is the sum-for-product step of the BBMW row), and the corollary `msep_eq_mackProcess_add_mackEstimation`. Tools: `Finset.prod_Ico_consecutive`, `Finset.prod_eq_prod_Ico_succ_bot`, `Finset.sum_Ico_eq_sum_range`, `procVar_eq_sum`. Still open: the paper's claims-development-result formulas between two arbitrary future horizons, and its own aggregation over accident years (`rohrMsepTotal` carries Mack's cross terms and is a definition, not an attribution).

11. DONE 2026-08-25 (Mack1999.lean): general weights and exponent, item 1 of the "good first theorems" list in CONTRIBUTING.md. `SW`, `TW`, `fhatW`, `sigma2W`, `ChatW`, `ultimateW`, `mackTermW`, `msepW` (Mack 1999 equation (*)) and `se2recW` (the recursion displayed below it) as definitions for arbitrary `w : ℕ → ℕ → ℝ` and arbitrary `α : ℕ`; `fhatW_eq_weighted_average`, `weighted_sq_devW`, `weighted_sq_devW_at_fhatW` (the deterministic identities in that generality); `se2recW_eq_closed` and `se2recW_eq_msepW` (the recursion equals (*) for every weight function and every exponent, given nonvanishing weighted development factors along the row); `SW_unit`, `TW_unit`, `fhatW_unit`, `sigma2W_unit`, `ChatW_unit`, `ultimateW_unit`, `mackTermW_unit`, `msepW_unit`, `se2recW_unit`, `se2recW_eq_msep_unit` (the α = 1, unit-weight case is `se2rec_eq_msep`); `condExp_fhatWrv` (conditional unbiasedness of the weighted estimator under (M1), generalizing `condExp_fhatRv`, with `RandomTriangle.gW` the `D_k`-measurable multiplier `w_ik C_ik^α / C_ik` that makes α = 0 and α = 2 work uniformly); `Mack3W` and `Mack2Factor'` (the weighted conditional second-moment and cross-year assumptions); `condExp_sq_fhatWrv_sub` and `condVar_fhatWrv` (`σ_k² / ∑_j w_jk C_jk^α` as the conditional squared error and, under conditional unbiasedness, the conditional variance); `condExp_wssWrv` and `condExp_sigma2Wrv` (conditional unbiasedness of the weighted residual sum of squares and `sigma2W`). Tools: `Finset.prod_Ico_succ_top`, `Finset.sum_Ico_succ_top`, `condExp_mul_of_stronglyMeasurable_left`, `condExp_finsetSum`, `Measurable.pow_const`. Still open: a witness triangle at α = 0 and α = 2, and an active-contributor version that omits zero-weight cells and uses active cardinality minus one for its degrees of freedom. The tail factor of the paper's Section 3 is a convention: `tailUltimate` and `tailSe2Step` are definitions and no theorem uses them.
12. DONE 2026-08-25 (ODP.lean, ODPStochastic.lean, Test/ODPStochasticWitness.lean): the deterministic score-equation layer is joined by the stochastic ODP layer. `ODPModel` couples `phi > 0`, a positive corner-constrained log-link mean, and square-integrable independent cells with `E[X_ik] = m_ik` and `Var(X_ik) = phi * m_ik`; `ODPModel.mean_pos` makes the implied mean positivity explicit. `condExp_odpCell_given_observed` and `condExp_sq_odpCell_sub_given_observed` are the exact conditional moments; `variance_odpAggregate`, `meanSquaredPredictionError_eq_variance_add` and `odpAggregateMsep_eq` give the exact finite-aggregate process and prediction-error identities. `glmCellMsep` and `glmAggregateMsep` transcribe the source's delta-method approximations as definitions only. `logLinkFit_corner_eq_multFit` proves equivalence with the positive multiplicative form under corner constraints. The Pearson residual, scale, adjustment, pseudo-data inversion and two-stage England-Verrall bootstrap are definitions, with the two inversion identities proved; no bootstrap validity theorem is asserted. The finite product witness has four genuinely independent random cells of mean and variance one, conditions `(0,1)` on the nonempty observed set `{(0,0)}`, and instantiates the exact two-cell MSEP decomposition. Tools: `iIndepFun_pi`, `measurePreserving_eval`, `condExp_indep_eq`, `IndepFun.variance_sum`, `variance_sub`, finite integration. Still open: distribution-specific process simulators and any theorem about bootstrap coverage or consistency; neither follows from the two ODP moments alone.
13a. DONE 2026-08-25 (CDRMsep.lean): the MSEP of the one-year claims development result, Merz and Wüthrich (2008, "Modelling the claims development result for solvency purposes", CAS E-Forum Fall 2008, pages 542-568). Exact layer: `cdrProcVar` and `condExp_sq_trueCDR`, `E[CDR_i(k)² | D_k] = (∏_{j>k} f_j)² σ_k² C_{i,k}` under (M1) and (M3), which is the paper's display (2.18), with `condVar_trueCDR` reading the same quantity as the conditional variance. Estimator layer as definitions, from displays (3.4)-(3.10): `mwPsi`, `mwPhi`, `mwGamma`, `mwDelta`, `mwVarCDR`, `mwProcessCDR`, `mwParamCDR`, `mwMsepCDR`, `mwMsepObsCDR`, and from (3.12)-(3.15) the aggregate `mwLambda`, `mwXi`, `mwCrossObs`, `mwCross`, `mwMsepTotalObsCDR`, `mwMsepTotalCDR`. Bridges: `S_succ_eq` (`S^{I+1}_j = S^I_j + C_{I-j,j}`, so next year's column sums are functions of today's triangle), `mw_term_split` and `mwMsepCDR_eq_display` (the definitions collapse to the paper's own display (3.17)), `mwVarCDR_eq_plugin` and `mwProcessCDR_eq_plugin` (the first development step is exactly the plug-in of the exact expression, the rest of the process term is the correction for the later steps), `sum_range_cdrProcVar_eq_procVar`, `sum_procVar_step_eq_procVar` and `condExp_sum_cdrProcVar_eq_procVar` (the one-year process variances telescope into Mack's `procVar` with no remainder), `mwMsepCDR_eq_mwMsepObsCDR_add_mwVarCDR` and `mwMsepObsCDR_le_mwMsepCDR` (display (3.11)), `mwMsepTotalCDR_sub_mwMsepTotalObsCDR` (display (3.16)). Tools: `condExp_congr_ae`, `condExp_smul`, `Finset.prod_eq_prod_Ico_succ_bot`, `Finset.sum_Ico_eq_sum_range`, `Filter.eventually_all_finset`, `procVar_eq_sum`. Still open: the linear approximation of the paper's appendix (A.1) stated as its own definition, so that the passage from the exact conditional MSEP to `mwMsepCDR` becomes a formalized step rather than a citation; the calendar-year filtration, in which the paper's one-year step is literally the step from `D_I` to `D_{I+1}`; the corresponding exact statement for the observable CDR; and a witness triangle instantiating the CDR theorems.

13b. DONE 2026-08-25 (ObservedData.lean): the observed-data sigma-algebra and the remaining hypotheses of the exact MSEP theorems. `RandomTriangle.obsSigma`, the join over accident years `i < n` of `rowSigma i (n-1-i)`, and `RandomTriangle.otherObs` as definitions; `obsSigma_eq_sup` splits the observed data at one accident year (the latest-diagonal analogue of `D_eq_sup`); `stronglyMeasurable_obsSigma_C`, `_Srv`, `_fhatRv`, `_ChatRv` show every chain-ladder estimator is a function of the observed cells, with no hypothesis, which is the `hPmeas` of `condMsep_eq`; `condExp_obsSigma_eq_rowSigma` and `condExp_D_eq_rowSigma` reduce both conditionings to the row's own observed history, and `condExp_obsSigma_eq_D`, `condExp_C_obsSigma_eq_D`, `condExp_sq_C_obsSigma_eq_D` are the `hfut1`, `hfut2` hypotheses of `condMsep_eq`; `condExp_cross_obsSigma_eq_zero` and `condCrossFree_of_rows` derive `CondCrossFree` for the true ultimates from independence across accident years by the argument of `mack2'_of_rows`. `condMsep_eq_of_rows` and `condMsepTotal_eq_of_rows` (with `sum_procVar_le_condMsepTotal_of_rows`) are then Mack's Theorem 3 and its Corollary in exact form from (M1row), (M3row), (M2), `RowsGenerateD` and square integrability, with no further hypothesis. Tools used: `iSup`/`le_iSup₂_of_le`, `condExp_sup_of_indep`, `condExp_mul_of_stronglyMeasurable_left`, `condExp_condExp_of_le`, `MemLp.integrable_mul`, `MemLp.condExp`. Instantiated on the eight-outcome witness (`Test/IndependenceWitness.lean`: `condExp_obsSigma_eq_D_of_witness`, `condCrossFree_obs_of_rows`). Still open: (M3row) on that witness, which would make `condMsep_eq_of_rows` non-vacuous there as well; a witness with more than one nondegenerate development step; and the observed-data form of the CDR theorems of `CDR.lean`.
13c. DONE 2026-08-25 (Mack1993Table1.lean): Mack's first numerical example as kernel-checked arithmetic. `taylorAshe` transcribes Table 1; the nine `taylorAshe_fhat_*` and eight `taylorAshe_sigma2_*` theorems give exact rational estimator values, and `taylorAshe_total_reserve_rounds` certifies the 18,681 thousand total in Table 2. The ninth variance value remains outside the theorem set because Mack defines it by extrapolation on p. 217. Tool: `norm_num` over `fhat`, `sigma2`, `reserve`, finite sums, and finite products.

14a. DONE 2026-08-25 (Mack1994Tests.lean): Mack's Appendix G and H model checks. The deterministic API ranks adjacent development-factor columns and counts median-split labels on calendar diagonals. `spearmanRho_eq_one_sub_six_mul_sum_sq_div` identifies the centered-rank correlation with equation (G4); `spearmanPermutationMean_eq_zero` and `spearmanPermutationVariance_eq` prove its exact uniform-permutation null moments. `calendarNullMean_eq_mackCalendarNullMean` and `calendarNullVariance_eq_mackCalendarNullVariance` derive equations (H1) and (H2) from finite binomial sums for every retained count. Aggregate variances and Normal cutoffs remain nominal definitions because the paper adds uncorrelatedness and approximation assumptions.
14b. DONE 2026-08-25 (LognormalCI.lean): Mack's moment-matched lognormal approximation. `mackLognormalSigma2` and `mackLognormalMu` define the parameters from an arithmetic mean and variance. `mackLognormal_mean_eq` and `mackLognormal_variance_eq` verify the standard lognormal moment expressions for positive mean and nonnegative variance. The approximation remains a modeling choice rather than a distribution theorem. Source correction: these formulas are in Mack's 1994 CAS Forum paper, Chapter 4, equation (10), pp. 118-119, not Section 5 of the 1993 ASTIN paper.
15. DONE 2026-08-25 (BFPriorUncertainty.lean): the random a priori ultimate component of Mack's 2008 BF prediction-error calculation. `RandomTriangle.bfReservePriorRv` multiplies the observed outstanding fraction by a random prior; `BFPriorIndependent` states exact independence between the sigma-algebra generated by that prior and the observed triangle. `condExp_bfReservePriorRv` gives the conditional mean, and `condVar_bfReservePriorRv` gives the conditional prior variance as the outstanding fraction squared times mathlib's `variance`. This specializes Mack's product-variance argument to the chain-ladder pattern already used by `bfReserve`; the exact independence hypothesis strengthens the paper's practical-independence description. Tools: `condExp_indep_eq`, `condExp_mul_of_stronglyMeasurable_left`, `MeasurableSpace.comap`.

16. DONE 2026-08-25 (BuhlmannStraub.lean): Bühlmann and Straub (1970), Section 6 and display (4). `buhlmannStraubWeightedMean`, `buhlmannStraubCredibility`, `buhlmannStraubEstimate`, and `buhlmannStraubLoss` are deterministic definitions. `buhlmannStraubEstimate_normal_eq`, `buhlmannStraubLoss_eq_at_estimate_add`, and `buhlmannStraubEstimate_minimizes` prove the normal equation, square completion, and minimization for a positive total quadratic coefficient. `buhlmannStraubEstimate_eq_credibility` gives the source form `Z X̄ + (1-Z)m`, and `buhlmannStraubCredibility_mem_Icc` puts `Z` in `[0,1]` under nonnegative inputs. Still open: a stochastic Bühlmann-Straub model deriving the loss from conditional moments, and estimation of the structural parameters.

17. DONE 2026-08-25 (MunichChainLadder.lean): Quarg and Mack (2004), Section 1.1.2, pp. 271-272 of the authorized English reprint. `sclQuadrangle` completes a paid or incurred cumulative triangle with its own chain-ladder projection, and `sclColumnTotal` sums all rows of that completed quadrangle. `sclColumnTotal_succ` proves that a completed column total evolves by the original chain-ladder factor; `sclColumnTotal_eq_mul_prod` iterates the step. `paidIncurredRatio` and `averagePaidIncurredRatio` are the row and completed-portfolio ratios. `quargMack_gap_identity` proves the boxed identity that separate chain ladder preserves each row's P/I ratio relative to the portfolio P/I ratio.
18. DONE 2026-08-25 (MunichStochastic.lean): Quarg and Mack (2004), Sections 2.2 and 3.1.2, pp. 284-292 of the authorized English reprint. `MunichTriangle` puts paid and incurred triangles on one probability space; `jointRowSigma`, `conditionalStdDev` and `conditionalResidual` define the information and residuals used by PQ and IQ. `MunichRowsIndep` states PIU for the finite `n` accident years through development `n - 1`. `MunichPaidRegression` and `MunichIncurredRegression` state PQ and IQ for adjacent periods `k + 1 < n`. `condExp_mul_eq_slope_of_residual_regression` proves that a standardized conditional regressor's slope is its residual cross-moment, and the paid and incurred corollaries apply it to PQ and IQ. The practical ratio-pattern, dispersion, residual, pooled-slope, adjusted-factor and coupled-projection formulas are definitions. `munichProjection_zero` proves that zero slopes give the two separate chain-ladder projections exactly. The correlation corollaries require the conditional second moment of the ratio residual to equal one explicitly; no consistency, convergence or prediction-error result is claimed for the practical estimators.
